-- figures.lua
-- Pandoc Lua filter: convert figure metadata blocks to \begin{figure}.
--
-- STUB — not yet functional. When the figure metadata format is defined
-- (via skills/figure_design), this filter will convert metadata blocks
-- in Markdown to proper LaTeX figure environments.
--
-- To activate: add --lua-filter=pandoc/filters/figures.lua to build.sh.

-- Placeholder: no-op filter
return {}
