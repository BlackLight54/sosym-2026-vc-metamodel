# Advisor consultation record, 2026-07-13

Martin showed the advisor the kickoff/SLR report (`context/reports/2026-07-13_kickoff_slr_report.html`),
the findings-first write-up of the proto-SLR agreed at the 2026-07-06 consultation and executed in
one session. This is the authoritative meeting record; feedback is processed via
`skills/plan_advisor_feedback` (decisions to D-notes, work to todos, lessons to memories).

## Reaction

Strongly positive. The advisor called the report "a gold mine". The direction is confirmed and no
content changes were requested: the corpus-first working method (find, verify, synthesize, then
report the findings before the method) landed exactly as intended, and the state-of-the-art
quadrant plus the five load-bearing inferences were received as the payoff.

## The one additional ask

Record the process itself as a reusable skill. The proto-SLR pipeline that produced the report
should be captured so it can be re-run for the remaining paper work, not reconstructed from memory
each time.

## Ruling batch (same day, 12 answers)

Martin issued twelve rulings alongside the review:

1. Next sessions run the **taxonomy** arm (AF01) and the **evaluation** arm in parallel.
2. The taxonomy arm gains a **targeted eIDAS 1 / enterprise-PKI finder extension** (non-web-PKI
   error history, the corpus covers only web PKI/TLS today).
3. The evaluation arm runs **T12 first, then AF02**.
4. Existing RQs are **enriched only**; no new RQ beyond the already-adopted RQ0.
5. **Full-text reads are per-use at drafting**, not a batch pass now.
6. **Coverage audit once now, then again before the spine.**
7. **Must-cite imports run in per-axis batches.**
8. **Zotero entries are entered manually by Martin** from the S-notes.
9. The **pipeline is recorded as skills**: a `plan_research_pipeline` meta-skill plus a process
   skill (the advisor's additional ask above).
10. The pipeline runs **fully lights-out, manually triggered**.
11. Subagents run as **Opus at full effort**, with **Fable reserved for synthesis stages** only.
12. Parallel tasks stay **file-disjoint** (the collision avoided this session: T12 and AF02 versus
    O-EUTHREAT on `05_evaluation.md`).

## Immediate implications (processed downstream, recorded here for provenance)

1. Findings persisted to the vault: `[[C-041 formality-scope-gap]]`, `[[C-042 ecosystem-level-lint]]`,
   `[[C-043 ecosystem-definitional-gap]]`, so nothing lives only in the report.
2. AF01 gains the eIDAS 1 / enterprise-PKI finder-plus-verifier arm (amendment appended to
   `context/todos/af01_design_error_taxonomy.md`).
3. Feedback memory `feedback_advisor_goldmine.md` records the verdict on the corpus-first method:
   repeat the cycle shape for the remaining paper work.
4. Pipeline-as-skills work is the advisor's standing request; captured for the next planning pass.
