\section{Motivation}\label{motivation}

\todo[inline]{Draft — Diploma scenario. Student GPA credential. JSON-LD discloses exact value; AnonCreds supports ZKP range proof. If a governance framework requires privacy-preserving verification but the credential format is JSON-LD, the design has a cross-layer inconsistency — invisible when inspecting any single layer.}

\todo[inline]{Draft — Alice scenario. Two credentials, shared subject. Entity alignment constraint spans claim→credential boundary. Reference teaser figure (right panel).}

\todo[inline]{Draft — single-layer inspection passes in both cases. Cross-layer errors only visible when layers are checked together.}

\todo[inline,color=blue!20]{cite: W3C VCDM 2.0 — structural constraints}

\todo[inline,color=blue!20]{cite: eIDAS 2.0 / ARF — format and disclosure constraints}

\todo[inline,color=blue!20]{cite: AnonCreds community guidelines}

\todo[inline]{Draft — multiple governance sources, not designed for joint satisfaction. Formal consistency not guaranteed.}

\todo[inline,color=blue!20]{cite: gap claim — verify no existing multi-level framework for VC ecosystems}

\todo[inline]{Draft — problem statement. Need formal framework for multi-layer, multi-source constraint satisfaction in VC ecosystems.}
