\todo[inline,color=gray!10]{meta: Dependencies: Section 04 (metamodel definitions and constraints).}

\section{Evaluation}\label{evaluation}

\todo[inline,color=purple!15]{scaffold: E0 --- Preamble --- evaluation strategy}

\todo[inline,color=purple!15]{scaffold: Job: State the two-part evaluation strategy and what each part demonstrates. Elaboration characterizes scope and expressiveness (qualitative). Measurement quantifies scalability (quantitative). Threats to validity addresses limitations. Be explicit about what the evaluation does NOT claim (no user study, no industrial deployment).}

\todo[inline]{Draft preamble --- two parts: (1) elaboration (scope, expressiveness, error visibility), (2) scalability measurement (runtime, model size). State scope and limitations upfront.}

\subsection{Elaboration}\label{elaboration}

\subsubsection{Metamodel Coverage}\label{metamodel-coverage}

\todo[inline,color=purple!15]{scaffold: E1 --- Soundness/completeness characterization}

\todo[inline,color=purple!15]{scaffold: Job: Characterize how far the formalization covers W3C VCDM 2.0. This is elaboration --- ``how far we got in the work'' --- not a measurement. Frame as soundness (every metamodel element traces to a VCDM concept) and completeness (which VCDM concepts are covered vs. excluded).}

\todo[inline,color=purple!15]{scaffold: Key content: Systematic mapping of VCDM 2.0 concepts to metamodel elements. Coverage table: VCDM concept {\textrightarrow} metamodel element {\textrightarrow} layer. Explicit exclusions with rationale (proof mechanisms, verifiable presentations, status/revocation).}

\todo[inline,color=purple!15]{scaffold: Binding claims: \#2 (grounding in VCDM 2.0), \#5 (coverage characterization).}

\todo[inline,color=blue!20]{cite: W3C VCDM 2.0 specification --- full concept list}

\todo[inline,color=orange!20]{figure fig\_coverage\_table: Coverage mapping table: VCDM 2.0 concept {\textrightarrow} metamodel element {\textrightarrow} layer. Mark in-scope / out-of-scope.}

\todo[inline]{Draft --- VCDM coverage as soundness/completeness characterization. N of M VCDM concepts mapped. Explicit exclusion list with rationale. Frame as ``how far we formalized,'' not as a validation metric.}

\subsubsection{Constraint Expressiveness}\label{constraint-expressiveness}

\todo[inline,color=purple!15]{scaffold: E4 --- Method}

\todo[inline,color=purple!15]{scaffold: Job: Describe how constraints were collected from governance sources and classified.}

\todo[inline,color=purple!15]{scaffold: Key content: Collected constraints from W3C VCDM 2.0 spec, EU eIDAS 2.0 / Architecture Reference Framework (ARF). Classified each as: expressible (with predicate name), partially expressible, or not expressible in the constraint language.}

\todo[inline,color=purple!15]{scaffold: Binding claims: \#6 (expressiveness vs. W3C + EU sources) --- HIGH RISK.}

\todo[inline,color=blue!20]{cite: W3C VCDM 2.0 --- normative requirements}

\todo[inline,color=blue!20]{cite: eIDAS 2.0 implementing regulation}

\todo[inline,color=blue!20]{cite: EU Architecture Reference Framework (ARF) v2.7.3}

\todo[inline,color=purple!15]{scaffold: E5 --- Results table}

\todo[inline,color=purple!15]{scaffold: Job: Present constraint expressiveness results. Focus on constraints that exercise the three-layer architecture.}

\todo[inline,color=purple!15]{scaffold: Key content: Table with columns: ID, constraint description, source, expressible (yes/partial/no), predicate/mechanism. Feature the ARF constraints that validate the metamodel's capabilities:}

\todo[inline,color=purple!15]{scaffold: --- C1 (PID\_02): Dual format mandate --- PID in both ISO 18013-5 and SD-JWT VC. YES --- EidasMandate + format class.}

\todo[inline,color=purple!15]{scaffold: --- C4 (ARB\_02): Proximity requires mdoc (SD-JWT VC lacks offline protocol). YES --- supports\_offline\_verification.}

\todo[inline,color=purple!15]{scaffold: --- C7 (ARB\_06): Encoding-independent attribute definition, then per-format. YES --- validates three-layer trace architecture.}

\todo[inline,color=purple!15]{scaffold: Plus W3C and community constraints already encoded in the Refinery model (governance\_conflict, cross\_cred\_predicate\_gap, trace consistency).}

\todo[inline,color=purple!15]{scaffold: Passing mention: ARF contains additional constraints (C2, C3, C5, C6, C8) that are partially expressible --- these mark metamodel scope boundaries, not failures. Details reserved for journal version.}

\begin{center}
\begin{tabular}{lllll}
\toprule
ID & Constraint & Source & Expressible & Predicate / Mechanism \\
\midrule
 &  &  &  &  \\
\bottomrule
\end{tabular}
\end{center}

\todo[inline,color=orange!20]{figure fig\_expressiveness\_table: Constraint expressiveness table: ID {\texttimes} constraint {\texttimes} source {\texttimes} expressible {\texttimes} predicate.}

\todo[inline]{Draft --- expressiveness table. Include ARF constraints C1, C4, C7 (fully expressible) plus W3C/community constraints from the Refinery model. Mention remaining ARF constraints (partially expressible) as scope boundaries in a brief paragraph --- journal material, not individual rows.}

\todo[inline,color=purple!15]{scaffold: E7 --- Limitations of expressiveness}

\todo[inline,color=purple!15]{scaffold: Job: State what the constraint language cannot express.}

\todo[inline,color=purple!15]{scaffold: Key content: Runtime behavioral constraints (e.g., revocation timing), policy-level access control, holder-binding protocols, issuance workflow constraints. The formalization covers structural and semantic constraints, not operational ones. Note: ARF constraints C2, C3, C5, C6, C8 are partially expressible --- attestation qualification hierarchy, per-claim SD annotation, and SD mechanism distinction are metamodel scope choices, not fundamental limitations.}

\todo[inline]{Draft --- expressiveness limitations. What falls outside the constraint language. Briefly note partially expressible ARF constraints as scope boundaries.}

\subsubsection{Headline Results}\label{headline-results}

\todo[inline,color=purple!15]{scaffold: E6 --- Cross-governance conflict results --- TWO HEADLINE RESULTS}

\todo[inline,color=purple!15]{scaffold: Binding claims: \#8 (formally contradictory cross-framework constraints), \#9 (multi-layer errors invisible to single-layer).}

\paragraph{Headline 1: Income governance conflict (vertical)}\label{headline-1-income-governance-conflict-vertical}

At the credential schema layer, IncomeCred is well-formed: \(\text{CS\_Applicant}_3\) traces to Applicant, \(\text{earns}_1\) traces to the \(\text{earns}\) property, \(\text{monthly\_income}_1\) traces to its value. All structural constraints (C1--C3) are satisfied.

At the format-specific layer, three governance sources impose requirements on IncomeCred:

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{eIDAS ARF} (normative, SHALL): \(\text{format}(\text{IncomeCred}) \in \{\text{SD-JWT-VC}, \text{mdoc}\}\) --- neither supports predicate proofs \todo[color=blue!20]{cite: eIDAS ARF --- dual format mandate}.
\item
  \textbf{GDPR Art. 5(1)(c)} (operationally binding): the income threshold check requires disclosing only whether \(\text{monthly\_income} \geq \text{threshold}\), not the exact value. If data minimization is to be achieved through technical means at the credential layer, this requires predicate proof capability \todo[color=blue!20]{cite: GDPR Art. 5(1)(c)}. The Hungarian data protection authority has enforced this interpretation in the housing subsidy context specifically.\footnote{NAIH fined a bank 35M HUF for excessive data collection during CSOK applications.}
\item
  \textbf{W3C VCDM 2.0}: the credential format must conform to the VCDM data model --- AnonCreds v1 does not (no \texttt{@context}, no \texttt{credentialSubject} structure, CL signatures not a registered proof type) \todo[color=blue!20]{cite: W3C VCDM 2.0} \todo[color=blue!20]{cite: AnonCreds specification}.
\end{enumerate}

No format satisfies all three requirements. SD-JWT-VC satisfies (1) and (3) but not (2). AnonCreds satisfies (2) but not (1) or (3). The configuration is unsatisfiable: constraints C5, C6, and C7 cannot be simultaneously satisfied on IncomeCred.

This contradiction is invisible to single-layer inspection. At the credential schema layer alone, IncomeCred is well-formed. At the format-specific layer under eIDAS alone, SD-JWT-VC is compliant. At the format-specific layer under GDPR alone, AnonCreds provides the needed capability. Only the joint, cross-governance, cross-layer analysis reveals the conflict.

\todo[inline]{After Refinery formalization (Pass 2) --- show the error predicates firing and the model generator producing no valid instance.}

\emph{Remark.} An issuer-precomputed boolean claim (\(\text{income\_above\_threshold}: \text{true}\)) can approximate a predicate proof within SD-JWT-VC. However, this workaround requires the issuer to anticipate every verifier threshold at issuance time, produces combinatorial explosion for multi-threshold scenarios, and remains static --- a credential issued with threshold \(X\) cannot serve a verifier requiring threshold \(Y\) without reissuance. As shown in Section 4.4, this workaround restructures the domain concept layer --- itself a cross-layer propagation that confirms the need for multi-layer analysis.

\paragraph{Headline 2: Cross-credential predicate gap (horizontal)}\label{headline-2-cross-credential-predicate-gap-horizontal}

The domain constraint \(\text{property\_area} \geq \text{min\_area}(\text{num\_children})\) (C4) requires combining values from two credentials issued by independent authorities: \(\text{property\_area}\) from PropertyCred (land registry) and \(\text{num\_children}\) from FamilyStatusCred (civil registry).

No deployed credential format supports cross-credential arithmetic predicates in zero-knowledge:

\begin{center}
\begin{tabular}{llll}
\toprule
Format & Single-cred predicate & Cross-cred equality & Cross-cred arithmetic \\
\midrule
AnonCreds v1 (CL) & Yes (attr \(\geq\) const) & No & \textbf{No} \\
AnonCreds v2 (BBS/PS) & Yes (range proofs) & Yes & \textbf{No} \\
SD-JWT-VC & No & No & \textbf{No} \\
SNARK-based \%\% @CITE: zk-creds, IEEE S\&P 2023 \%\% & Yes & Yes & Yes (research prototype) \\
\bottomrule
\end{tabular}
\end{center}

To verify the floor area constraint, the verifier must see both raw values from two separate credentials, defeating the privacy properties that ZKP-capable formats promise. The metamodel captures this: a domain-concept-layer constraint (C4) that spans credentials cannot be enforced privacy-preservingly at the format-specific layer because no deployed format supports cross-credential predicate proofs (C9).

This gap is again invisible to single-layer inspection: the domain concept layer constraint is well-defined, both credentials are well-formed at the credential schema layer, and each credential's format is individually valid at the format-specific layer. Only the cross-layer analysis --- checking whether the DCL constraint can be enforced given the FSL format capabilities --- reveals the expressiveness gap.

\emph{Complementarity.} The two results are orthogonal. Headline 1 identifies a \emph{vertical} governance conflict: contradictory requirements on a single credential's format from different regulatory sources. Headline 2 identifies a \emph{horizontal} expressiveness gap: an ecosystem-level constraint spanning credentials that exceeds any single format's capabilities. Together, they demonstrate that multi-layer analysis detects both governance conflicts and format expressiveness gaps invisible to single-layer inspection.

\subsubsection{Anti-Pattern Detection}\label{anti-pattern-detection}

\todo[inline,color=purple!15]{scaffold: E8 --- Anti-pattern catalog}

\todo[inline,color=purple!15]{scaffold: Job: List the known credential design anti-patterns and their error predicates.}

\todo[inline,color=purple!15]{scaffold: Key content: 3-5 anti-patterns drawn from the Refinery model. Each has a name, description of the design error, the error predicate that catches it, and which layer(s) it spans.}

\todo[inline,color=purple!15]{scaffold: Binding claims: \#7 (error detection vs. anti-patterns), \#9 (multi-layer errors invisible to single-layer).}

\todo[inline,color=orange!20]{figure fig\_antipattern\_table: Anti-pattern table: name {\texttimes} description {\texttimes} error predicate {\texttimes} layer(s) {\texttimes} detected?}

\todo[inline]{Draft --- anti-pattern catalog. At least 4 anti-patterns: bad\_trace, noSubject/root\_ent\_doesnt\_have\_cred, no\_cred\_props\_for\_cred\_entity, non\_connected, common\_parent.}

\todo[inline,color=purple!15]{scaffold: E9 --- Detection results}

\todo[inline,color=purple!15]{scaffold: Job: For each anti-pattern, show whether the error predicate catches it and demonstrate with the running example.}

\todo[inline,color=purple!15]{scaffold: Key content: Construct a variant of the housing subsidy example that contains each anti-pattern. Show the Refinery error predicate firing. State which layer the error manifests in and whether single-layer inspection would catch it.}

\todo[inline]{Draft --- detection results per anti-pattern. Construct error variants of housing subsidy example.}

\todo[inline,color=purple!15]{scaffold: E10 --- The multi-layer visibility argument}

\todo[inline,color=purple!15]{scaffold: Job: Deliver binding claim \#9 --- demonstrate a specific error that passes single-layer checks but fails the cross-layer constraint.}

\todo[inline,color=purple!15]{scaffold: Key content: Construct a specific model where the domain concept layer is valid (connected, well-formed) and the credential schema layer is valid (all credentials have subjects, all claims have source/target), but the trace mapping is inconsistent (a CredentialSubject traces to a Value instead of a Subject). Show that this passes layer-by-layer checks but violates the cross-layer trace consistency constraint.}

\todo[inline]{Draft --- multi-layer visibility. The key demonstration: layer-by-layer checks pass, cross-layer check fails. This is the paper's core argument for why multi-level modeling is necessary.}

\subsubsection{Baseline Comparison}\label{baseline-comparison}

\todo[inline,color=purple!15]{scaffold: E11 --- Baseline comparison}

\todo[inline,color=purple!15]{scaffold: Job: Compare the Refinery-based approach with at least one baseline development tool to justify the tooling choice.}

\todo[inline,color=purple!15]{scaffold: Key content: Select one baseline (OCL on Ecore, Alloy, or manual inspection / spreadsheet-based design review). Compare on at least one axis: expressiveness (what constraints can be stated), automation (what the tool checks vs. what requires manual inspection), error visibility (does the tool surface cross-layer issues). Be honest about Refinery limitations (learning curve, tool maturity, graph-based reasoning overhead).}

\todo[inline,color=purple!15]{scaffold: Binding claims: addresses Reviewer D's ``tool dependency'' attack and Oszkr's ``why Refinery instead of X?'' question.}

\todo[inline]{Draft --- baseline comparison. Select one baseline tool. Compare on expressiveness and/or automation. Acknowledge Refinery limitations honestly.}

\subsection{Scalability Measurement}\label{scalability-measurement}

\todo[inline,color=purple!15]{scaffold: S0 --- Preamble}

\todo[inline,color=purple!15]{scaffold: Job: State the measurement objective: quantify how the approach scales with model size. Fixed schema: RQs {\textrightarrow} domains {\textrightarrow} setup {\textrightarrow} results {\textrightarrow} analysis.}

\subsubsection{Research Questions}\label{research-questions}

\todo[inline,color=purple!15]{scaffold: S1 --- Research questions}

\todo[inline,color=purple!15]{scaffold: Key content:}

\todo[inline,color=purple!15]{scaffold: RQ1: ``How does constraint validation runtime scale with model size (number of entities and constraints)?''}

\todo[inline,color=purple!15]{scaffold: RQ2: ``How does design space exploration (model generation) runtime scale with model size?''}

\todo[inline]{Draft RQ1 and RQ2. Consider whether a third RQ on error identification time or memory usage adds value.}

\todo[inline]{Placeholder --- additional RQs or metrics may be identified during measurement.}

\subsubsection{Selected Domains}\label{selected-domains}

\todo[inline,color=purple!15]{scaffold: S2 --- Model instances}

\todo[inline,color=purple!15]{scaffold: Job: Define 2-5 model instances of increasing size for scalability measurement.}

\todo[inline,color=purple!15]{scaffold: Key content: Based on CSOK variants. E.g., (1) 1-credential minimal, (2) 3-credential CSOK as-is, (3) 5-credential extended (add HealthCred, EmploymentCred), (4) 10-credential synthetic. Justify scaling: each additional credential adds ~N entities (CredentialSchema, CredentialSubject, Claims), ~M trace links, ~K governance annotations.}

\todo[inline]{Create 2--5 model instances of increasing size in models/ for scalability measurement. Base on CSOK variants.}

\subsubsection{Measurement Setup}\label{measurement-setup}

\todo[inline,color=purple!15]{scaffold: S3 --- Environment and methodology}

\todo[inline,color=purple!15]{scaffold: Key content: Refinery version, JVM version, hardware spec (CPU, RAM). Number of repetitions per measurement. Warm-up runs. Metrics: validation time (ms), model generation time (ms), memory usage (MB).}

\todo[inline,color=blue!20]{cite: <> --- Nour's ECMFA paper for measurement methodology inspiration. Martin to provide reference.}

\todo[inline]{Draft measurement setup. Include reproducibility information. Cite Nour's ECMFA paper for methodology.}

\todo[inline]{Placeholder --- additional metrics beyond runtime and memory may be added.}

\subsubsection{Measurement Results}\label{measurement-results}

\todo[inline,color=orange!20]{figure fig\_scalability\_validation: Validation runtime vs. model size (number of entities/constraints). X-axis: model size. Y-axis: time (ms). Lines for consistency check, error identification. figure* (full-width).}

\todo[inline,color=orange!20]{figure fig\_scalability\_generation: Generation runtime vs. model size. X-axis: model size. Y-axis: time (ms). Line for design space exploration. figure* (full-width).}

\todo[inline]{Run scalability measurements in Refinery and populate figures.}

\todo[inline]{Placeholder --- additional result figures if new metrics are added.}

\subsubsection{Analysis of Results}\label{analysis-of-results}

\todo[inline,color=purple!15]{scaffold: S4 --- Analysis}

\todo[inline,color=purple!15]{scaffold: Job: Answer each RQ based on measurement data. Draw conclusions about practical applicability. State limitations of the measurement (single tool, synthetic scaling, no comparison with industrial-scale ecosystems).}

\todo[inline]{Draft analysis --- answer RQ1 and RQ2, state practical implications and measurement limitations.}

\subsection{Threats to Validity}\label{threats-to-validity}

\subsubsection{Internal Validity}\label{internal-validity}

\todo[inline,color=purple!15]{scaffold: T1 --- Internal validity threats}

\todo[inline,color=purple!15]{scaffold: Key content:}

\todo[inline,color=purple!15]{scaffold: --- Constraint selection bias: constraints were chosen to demonstrate the metamodel's capabilities, not sampled randomly from governance documents. Mitigation: constraints come from normative sources (ARF, VCDM spec), not invented.}

\todo[inline,color=purple!15]{scaffold: --- Running example design: the CSOK scenario was constructed to exhibit both a vertical governance conflict and a horizontal expressiveness gap. A real-world ecosystem may not exhibit both simultaneously. Mitigation: both conflict types are independently documented in the literature.}

\todo[inline]{Draft internal validity threats.}

\subsubsection{External Validity}\label{external-validity}

\todo[inline,color=purple!15]{scaffold: T2 --- External validity threats}

\todo[inline,color=purple!15]{scaffold: Key content:}

\todo[inline,color=purple!15]{scaffold: --- Single domain: evaluation uses one running example (Hungarian housing subsidy). Results may not generalize to other credential ecosystems (healthcare, education, travel). Mitigation: the metamodel is defined generically; the CSOK instance demonstrates applicability, not exhausts it.}

\todo[inline,color=purple!15]{scaffold: --- EU regulatory focus: governance constraints drawn primarily from eIDAS/ARF. Other regulatory frameworks (NIST, ISO) may impose different constraint patterns.}

\todo[inline]{Draft external validity threats.}

\subsubsection{Construct Validity}\label{construct-validity}

\todo[inline,color=purple!15]{scaffold: T3 --- Construct validity threats}

\todo[inline,color=purple!15]{scaffold: Key content:}

\todo[inline,color=purple!15]{scaffold: --- Metamodel scope choices: the formalization excludes proof mechanisms, verifiable presentations, status/revocation. These exclusions are deliberate (structural vs. cryptographic concerns) but limit what constraints can be expressed. ARF constraints C2, C3, C5, C6, C8 are partially expressible due to missing attestation qualification hierarchy, per-claim SD annotation, and SD mechanism distinction --- these are scope choices, not fundamental limitations.}

\todo[inline,color=purple!15]{scaffold: --- Expressibility classification: ``partially expressible'' is a judgment call. Different metamodel designs might express the same constraint fully or not at all.}

\todo[inline]{Draft construct validity threats. Include metamodel scope choices from ARF gap analysis.}

\subsubsection{Conclusion Validity}\label{conclusion-validity}

\todo[inline,color=purple!15]{scaffold: T4 --- Conclusion validity threats}

\todo[inline,color=purple!15]{scaffold: Key content:}

\todo[inline,color=purple!15]{scaffold: --- Scalability measurement on synthetic instances: scaling model instances by adding credentials is representative of ecosystem growth, but does not capture complexity growth from richer governance frameworks or cross-ecosystem interoperability.}

\todo[inline,color=purple!15]{scaffold: --- Tool-specific results: Refinery's performance characteristics may not generalize to other graph-based modeling tools. The contribution is the metamodel and constraint formalization, not the tool.}

\todo[inline]{Draft conclusion validity threats.}
